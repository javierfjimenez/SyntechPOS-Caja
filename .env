# Entorno local de desarrollo (Vite expone solo variables VITE_*)
# Backend SyntechPOS servido por Herd/Valet; el fallback del código es
# http://localhost:8000/api/v1 (composer dev) — ver src/api/client.ts
VITE_API_URL=https://server.test/api/v1
